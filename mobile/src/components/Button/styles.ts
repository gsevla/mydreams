import { StyleSheet } from "react-native";

export const buttonStyles = StyleSheet.create({
  default: {
    padding: 16,
    height: 48,
    width: 92,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 8,
  },
});
